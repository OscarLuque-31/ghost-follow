spring:
  application:
    name: notifier-service
  config:
    # URL del Config Server (8888)
    import: configserver:http://localhost:8888
  cloud:
    config:
      import-check:
        enabled: false